# OpenCV gallery settings
# Relative paths are resolved from the directory where gallery.py is run.
gallery_dir=gallery
window_name=Gallery
wrap_enabled=false
wrap_counts=12
frame_delay_ms=1
menu_hide_seconds=3
video_format=mp4
video_codec=mp4v
